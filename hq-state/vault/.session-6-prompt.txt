You are the ICP Research Sub-Agent. Vault: C:\Users\Anthony\Documents\ICP-Vault\.

SESSION 6 — evaluate a deflationary-burn + staking-APY proposal layered on the Session 5 100M / 15 / 30 / 55 split.

Narrate progress — after each step, print [step N/9 done: X].

USER PROPOSAL (evaluate explicitly):
On top of the Session 5 allocation (100M total; 15M dev, 30M swap, 55M treasury):
- Burn schedule: 5M tokens from treasury every 6 months (10M / year).
- Staking APY: 8% initial, dropping to 2% after 4 years.
- Stated goal: deflationary net supply.

Key assumption to flag prominently: the 8% to 2% is assumed to be the SNS voting reward rate curve (minted, inflationary), NOT paid from treasury. If the user confirms treasury funds rewards instead, the math changes — cover that as a secondary scenario.

STEP 1 — context load
Read: AGENT-CHARTER.md, 06-Daily-Learning/2026-04-21-session-5.md, 05-Projects/tokenomics-100m-analysis.md, 01-Concepts/sns-tokenomics.md, sns-operations.md, sns-launch-benchmarks.md. Print [step 1/9 done: context loaded].

STEP 2 — new concept notes in 01-Concepts/ using templates/concept.md:

- sns-supply-dynamics.md — the three supply flows: minting (voting rewards, issuance), burning (fee burns, treasury burns, burn-address transfers), treasury movements (not a supply change, just accounting). ICRC-1 total_supply accounting. Circulating vs total. Cross-link [[sns-tokenomics]], [[sns-voting-rewards]], [[sns-treasury-burns]].

- sns-voting-rewards.md — the SNS voting reward curve: initial_voting_reward_rate, final_voting_reward_rate, reward_rate_transition_duration_seconds, VP-weighted distribution, the fact these are MINTED (inflationary). Explain how an 8% to 2% over 4-year curve would be configured in sns_init.yaml. Be precise about field names ONLY if verifiable from github.com/dfinity/ic; otherwise qualitative + open_questions. Cross-link [[sns-init-config]], [[sns-neurons-and-voting]], [[sns-supply-dynamics]].

- sns-treasury-burns.md — how to actually burn tokens held by an SNS treasury. Mechanics: TransferSnsTreasuryFunds to ICRC-1 burn (transfer to minting account or canister-controlled burn address), governance proposal per burn or a scheduled-burn canister pattern. Governance risk: each burn needs a proposal; DAO can vote it down. Cross-link [[sns-operations]], [[sns-canisters]], [[sns-supply-dynamics]].

- sns-fee-burn-patterns.md — alternative deflationary mechanism: burn a % of every ledger fee / protocol revenue event. Compare/contrast with scheduled treasury burns. Benchmarks: BNB quarterly burns, ETH EIP-1559, canister-fee burn patterns. Cross-link [[sns-treasury-burns]], [[sns-supply-dynamics]].

Print [step 2/9 done: 4 supply-dynamics notes].

STEP 3 — math worksheet at 05-Projects/tokenomics-100m-v2-burn-and-staking.md using templates/project.md (status: planning, type: project, tags include icp/project icp/sns/tokenomics icp/sns/supply-dynamics).

Sections:
1. Proposal summary — the full Session 5 + Session 6 proposal in one paragraph.
2. Primary scenario: rewards minted (standard SNS) — year-by-year table for years 1 through 10:
   - Stake ratio assumptions: low (15%), base (30%), high (50%).
   - Annual minted rewards = stake_ratio * current_supply * reward_rate_that_year. Reward rate decays linearly 8% to 2% over years 0 to 4, then flat 2%.
   - Annual burns = min(10M, treasury_remaining).
   - Net supply change = minted minus burned.
   - Treasury balance at year-end.
   - Total supply at year-end.
   - Flag the year treasury is exhausted under each scenario.
3. Secondary scenario: rewards paid from treasury (non-standard) — same table structure but treasury also funds rewards. Show treasury depletion is dramatically faster.
4. Key findings (bullets, ranked):
   - Primary risk: treasury exhaustion at ~year 5.5 under burn plan alone; earlier if treasury also funds rewards.
   - Post-exhaustion the system is net-inflationary unless fee-burns kick in.
   - DAO has limited runway for grants / audits / incident response after ~year 4.
   - Each scheduled burn requires a proposal — schedule is aspirational, not enforced.
   - 8% starting APY is aggressive vs benchmarks (reference [[sns-launch-benchmarks]]).
5. Alternative proposal to compare:
   - Fee-burn / revenue-burn instead of fixed schedule (supply burns track usage).
   - Lower initial APY (e.g. 4-5%) to reduce inflation pressure.
   - Preserve treasury as a war chest; explicit treasury-use policy (grants %, ops %, burn %).
6. Recommendations — 5 to 8 concrete asks the user needs to answer/decide before committing:
   - Confirm rewards funding source (minted vs treasury).
   - Commit to vesting / dissolve-delay for dev 15%.
   - Re-plan treasury envelope so it survives 10+ years.
   - Decide between programmatic burns vs fee-burns vs hybrid.
   - Set explicit treasury policy: burn %, grants %, ops %, reserve %.
   - Define what triggers the APY step-down (calendar vs TVL milestone).
7. Open questions — list any unverified SNS config field names / CMC math / historical benchmark gaps.

Do NOT fabricate figures. Where a reasonable estimate is needed for the math, state the assumption explicitly in-line. Print [step 3/9 done: v2 analysis with math worksheet].

STEP 4 — refine existing tokenomics note
Edit 05-Projects/tokenomics-100m-analysis.md: at the top, add a superseded_by frontmatter key pointing to [[tokenomics-100m-v2-burn-and-staking]] and a one-sentence body note. Preserve the original content. Print [step 4/9 done: v1 analysis cross-referenced].

STEP 5 — benchmarks refresh
In 01-Concepts/sns-launch-benchmarks.md, add a column or section for Reward rate curve (initial to final over N years) for each SNS row where you can verify it. Leave ? where you cannot. Print [step 5/9 done: benchmarks refreshed].

STEP 6 — refine sns-tokenomics.md
Add a section titled Net supply = mint minus burn that explains the two flows. Cross-link [[sns-voting-rewards]], [[sns-treasury-burns]], [[sns-fee-burn-patterns]], [[sns-supply-dynamics]]. Promote to stable if content is well-sourced; else stay draft. Print [step 6/9 done: tokenomics note refined].

STEP 7 — MOC update
00-Index/icp-moc.md — under Governance add checkmark for [[sns-supply-dynamics]], [[sns-voting-rewards]], [[sns-treasury-burns]], [[sns-fee-burn-patterns]]. Under Projects add checkmark for [[tokenomics-100m-v2-burn-and-staking]]. Update updated: frontmatter with Session-6 note. Print [step 7/9 done: MOC updated].

STEP 8 — session log
Create 06-Daily-Learning/2026-04-21-session-6.md. Full log frontmatter (tags icp/log icp/sns/tokenomics icp/sns/supply-dynamics). Include:
- User proposal exactly as given.
- TL;DR (3 to 5 bullets): the key findings from the math worksheet.
- Assumed funding source for rewards (minted); what changes if treasury-funded.
- Files created / refined with one-line deltas.
- Open questions.
- Three concrete next-session priorities.
Print [step 8/9 done: session log].

STEP 9 — learning log
Append one bullet under Recent Highlights in 00-Index/learning-log.md. Print [step 9/9 done: learning log].

STRICT RULES (same as prior sessions):
- YAML frontmatter on every .md.
- Hierarchical / tags; wiki-links only for internal refs; real URLs only.
- Dates 2026-04-21.
- Zero fabrication. No invented supply figures, reward-rate field names, or benchmark numbers. Flag every assumption. Prefer ? or open_questions over guessing.
- Stay inside C:\Users\Anthony\Documents\ICP-Vault\.
- WebFetch dashboard.internetcomputer.org, internetcomputer.org/docs, github.com/dfinity/ic, forum.dfinity.org when verifying.

Final output: summary of files + math worksheet TL;DR (year treasury exhausts under each scenario) + the clarifying question the user needs to answer about the rewards funding source + three next-session priorities.
