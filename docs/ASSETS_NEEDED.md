# Frontend image assets — status & what to replace

The SvelteKit Pages/HQ frontend references images at `/assets/<name>`. Those files
must live in the repo-root **`assets/`** directory; `frontend/scripts/sync-static-assets.mjs`
(wired as `prebuild`/`predev`) mirrors them into `frontend/static/assets/` so the
asset canister (ai.cafreso.com / v4tdv) serves them. **Drop a real file into repo-root
`assets/` and the next build publishes it automatically** — no code change needed.

## Why images were broken on v4tdv (root cause)
Components referenced `/assets/*.png`, but the image files lived only at repo-root
`assets/` (used by the container / hq-ui build) and were never copied into the
frontend's `static/` dir. The SvelteKit build only ships `frontend/static/`, so every
`/assets/*` request 404'd. Two real marks (`cafreso-wordmark.png`, `cf-black.png`) even
existed in `static/` but at the root (`/cafreso-wordmark.png`), not under `/assets/`.
Fixed by the `prebuild` sync above. (It was never a CSP issue — `img-src` allows them.)

## Real assets (shipping as-is) ✅
| File | Notes |
|---|---|
| `cafreso-wordmark.png` | Cafreso wordmark (real) |
| `cf-black.png` | Cafreso mark, black (real) |
| `cf-mark-coffee.png`, `cf-mark-white.png` | Cafreso marks (real) |
| `apple-touch-icon*.png`, `favicon-16/32.png`, `icon-192/512.png` | PWA/favicons (real) |

## PLACEHOLDERS — replace with final artwork 🟡
Generated by `scripts/gen_placeholder_assets.py` (clean on-brand tiles / honest text
badges — **not** final art). Regenerate any with `python3 scripts/gen_placeholder_assets.py <name>`.

| File | Used by | Suggested final art | Px (placeholder) |
|---|---|---|---|
| `nanas-coin.png` | PricePill, CartDrawer, store (×12) | $nanas coin icon (transparent PNG) | 256² |
| `banking-brave-logo.png` | BankingBravePost (×7) | Banking.Brave logo | 256² |
| `icp.png` | Footer, OnChainBadge, BankingBravePost (×6) | Internet Computer logo | 256² |
| `cafreso-roaster.png` | blog/products hero, design page | Roaster product photo | 512² |
| `cf-gold.png` | Footer, blog (celebratory mark) | Gold Cafreso mark | 256² |
| `cafreso.png` | blog "farm" hero, design page | Pixel-art vertical-farm hero | 800×400 |
| `gold-dao.png` | homepage partner ribbon | Gold DAO logo | 256² |
| `not-yet-available.png` | products fallback image | "Coming soon" product tile | 512² |
| `open-chat.jpg` | Footer link | OpenChat logo | 256² |

## To replace a placeholder
1. Put the real file (same name) into repo-root `assets/`.
2. `npm --prefix frontend run build` (prebuild sync copies it into `static/assets/`).
3. Deploy: `dfx deploy cafresoai_frontend --network ic --identity default`.
